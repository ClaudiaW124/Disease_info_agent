model_provider:
  type: ${oc.env:UTU_LLM_TYPE}
  model: ${oc.env:UTU_LLM_MODEL}

  # NOTE: we should set these envs in .env
  # base_url: ${oc.env:UTU_LLM_BASE_URL}
  # api_key: ${oc.env:UTU_LLM_API_KEY}

model_settings:
  # Qwen3 默认开思考模式，免费档排队时很容易超过客户端超时
  extra_body:
    enable_thinking: false
